advanced 1.2.0
-> ajax to js <-> jsp (not inner but external)

dynamic 1.3.0
FragmentBox?
server in static site? 
Dom to class?
can make customTag to 
{customTag} extends {HTMLElement} {
	action? = functional;
	onclick = ..;
}

example
myButton extends input {
// value (input)
// type (input)
	href = ...()
}
: can override parent action (value, type...)
: can create custom action (myaction, ...)
